(*
  This combinator probably needs a better name, but I can't come up with one.  The idea is that 
  it takes two sub-UIs and renders the first, followed by the second, after applying an 
  arrangement that depends on the size of the first UI.

module Reduced

type Model<'e1, 'e2, 'm1, 'm2> = {
    
}
*)
